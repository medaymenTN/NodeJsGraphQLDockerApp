import { Db } from 'mongodb';
export declare class Database {
    db: Db;
    constructor(db: Db);
    insertDocumentsIntoCollection(documentsToInsert: any[], collectionName: string): Promise<import("mongodb").InsertWriteOpResult>;
    drop(): Promise<any>;
    ifCollectionExist(collectionName: string): Promise<boolean>;
    dropCollectionIfExists(collectionName: string): Promise<any>;
}
