import { SeederCollection } from '../common';
export declare type CollectionTransformerFn = (c: SeederCollection) => SeederCollection;
export declare const DefaultTransformers: {
    replaceDocumentIdWithUnderscoreId: (collection: SeederCollection) => SeederCollection;
};
