import { Database } from '../database';
import { SeederCollection } from '../common';
export declare class CollectionImporter {
    db: Database;
    constructor(db: Database);
    import(collections: SeederCollection[]): Promise<void>;
    private importCollection;
}
